#pragma once

#include <data.pb.h>
#include <functional>
#include <memory>

namespace virtdb { namespace datasrc {
  
  class pool
  {
    // - allocate
    // - dispose
    // - wait all disposed (when all sent)
    // - allocated count
    // - disposed count
  };
}}
